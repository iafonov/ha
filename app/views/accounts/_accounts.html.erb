<%= stylesheet_link_tag "accounts" %>

<div class="manager" id="accounts-manager">
  <h1>Accounts</h1>
  <div id="accounts"> 
    <ul id="accounts-list"></ul> 
  </div>
  <div id="create-account"> 
    <%= label_tag 'new-account-name', 'Create new account:' %>
    <%= text_field_tag 'new-account-name', nil, :class => 'new', :placeholder => 'New account name' %>
    <br />
    <%= label_tag 'new-account-name', 'Currency:' %>
    <%= select_tag 'new-account-currency', options_for_select(Account::VALID_CURRENCIES) %>
    <br />
    <%= submit_tag "Add", :id => "create-new-account" %>
  </div>

  <script type="text/template" id="item-template"> 
    <div class="item">
      <span class="account-name">{{name}}</span>
      <span class="account-currency">{{currency}}</span>
      <span class="destroy">X</span>
    </div>
    <div class="edit">
      <%= text_field_tag 'account-name-input', nil, :class => 'account-name-input' %>
      <%= select_tag 'account-currency-input', options_for_select(Account::VALID_CURRENCIES), :class => 'account-currency-input' %>
      <span class="action-link save">Save</span>
      <span class="action-link cancel">Cancel</span>
    </div>
  </script>

  <%= javascript_include_tag "accounts" %>
</div>